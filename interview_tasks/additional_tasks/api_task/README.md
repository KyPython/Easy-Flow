API Change Task — Add validation to `create_user` function

Goal:
- Implement `create_user(data)` that validates input and returns a user dict with `id`, `name`, and `email`.

Timebox: 45 minutes

Run tests:

```bash
pytest interview_tasks/additional_tasks/api_task/tests/test_api.py -q
```
