Performance Task — Optimize word frequency counting

Goal:
- Replace a naive O(n^2) counting implementation with an O(n) solution. Add a simple benchmark.

Timebox: 45 minutes

Run tests:

```bash
pytest interview_tasks/additional_tasks/perf_task/tests/test_perf.py -q
```
