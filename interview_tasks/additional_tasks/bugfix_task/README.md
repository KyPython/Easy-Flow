Bugfix Task — Deduplicate list while preserving order

Goal:
- Implement `remove_duplicates(seq)` that returns a list with duplicates removed while preserving first-seen order.

Timebox: 30 minutes

Run tests:

```bash
pytest interview_tasks/additional_tasks/bugfix_task/tests/test_bugfix.py -q
```
