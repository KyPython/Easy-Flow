#!/usr/bin/env sh

# EasyFlow Pre-Commit Hook
# Runs quick validation checks before commits
# Non-blocking for tests (they'll block in pre-push)

echo "🔍 Running pre-commit checks..."

npm run pre-commit
