global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'easyflow-app'
    static_configs:
      - targets: ['localhost:9090']

# Example alert rule: high error rate
rule_files:
  - 'alerts/easyflow-alerts.yml'
