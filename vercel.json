{
  "version": 2,
  "buildCommand": "cd rpa-dashboard && npm run build",
  "installCommand": "cd rpa-dashboard && (npm ci --include=dev --silent --no-audit --no-fund --loglevel=error > /dev/null 2>&1 || npm ci --include=dev --silent --no-audit --no-fund)",
  "outputDirectory": "rpa-dashboard/build",
  "framework": null
}

