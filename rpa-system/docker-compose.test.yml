version: "3.8"

# This file overrides settings in docker-compose.yml for testing purposes.
# To use it, run: docker-compose -f docker-compose.yml -f docker-compose.test.yml up
# To drop it, run: docker-compose -f docker-compose.yml -f docker-compose.test.yml down

services:
  email_worker:
    environment:
      # Point the email worker to the test probe container for capturing emails.
      - SEND_EMAIL_WEBHOOK=http://hooks_probe:4001/api/send-email-now