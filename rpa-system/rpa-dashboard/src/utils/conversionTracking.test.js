// Placeholder Jest test to avoid "must contain at least one test" error
describe('conversionTracking (placeholder)', () => {
  test('placeholder test', () => {
    expect(true).toBe(true);
  });
});

// The browser-only manual helper was moved to `conversionTracking.manual.js`.
// If you need to run manual checks, open the app in a browser and run:
//   testConversionTracking()