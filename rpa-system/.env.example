## Root .env for Docker Compose (server-only). Copy to .env and fill CHANGEME_.* values.

# Supabase (server only). Get from Supabase project settings.
AUTOMATION_BASE_URL=http://localhost:7001
SUPABASE_URL=https://syxzilyuysdoirnezgii.supabase.co
SUPABASE_BUCKET=artifacts
ALLOWED_ORIGINS=https://app.34.171.164.208.sslip.io
SUPABASE_PASSWORD=A_&XTnEzW58PpQq
SUPABASE_USE_SIGNED_URLS=true
SUPABASE_SIGNED_URL_EXPIRES=86400
POSTGRES_URL=postgresql://postgres:[A_&XTnEzW58PpQq]@db.syxzilyuysdoirnezgii.supabase.co:5432/postgres
PORT=3001

# Storage options
SUPABASE_BUCKET=artifacts
SUPABASE_USE_SIGNED_URLS=true
SUPABASE_SIGNED_URL_EXPIRES=86400

# Backend port inside container (do not change) and host mapping done in compose
PORT=3001

# Backend -> Automation URL (internal Docker network). Do not expose automation publicly in prod.
# Default is set in docker-compose.yml; override here only if needed.
# AUTOMATION_URL=http://automation:7001/run

# Optional API key to protect /api routes. If set, clients must send header x-api-key: <value>
API_KEY=CHANGEME_API_KEY

# CORS whitelist (comma-separated). Example: https://app.example.com,https://admin.example.com
# Default for free SSL host (sslip.io) bound to your VM IP
ALLOWED_ORIGINS=https://app.34.171.164.208.sslip.io
